# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Easy EV Chager' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Easy EV Chager

  target 'Easy EV ChagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Easy EV ChagerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
